server:
  port: 8080

auth:
  api_keys:
    - "key-abc-123"
    - "key-xyz-789"

rate_limit:
  requests_per_second: 10
  burst: 20

routes:
  - prefix: "/api/users"
    backend: "https://httpbin.org"
  - prefix: "/api"
    backend: "https://httpbin.org"
